# Result viewer

The website should consume the same rich event, trajectory, and risk output as the pipeline. A dummy JSON fixture may support UI work before samples arrive, but must be clearly labeled demo data.
