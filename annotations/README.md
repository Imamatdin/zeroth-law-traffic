# Annotations

Store only labels and camera maps permitted by the organizer's data license. Keep raw videos in ignored `data/`. Document reviewer, camera, and label definitions with each annotation set.
